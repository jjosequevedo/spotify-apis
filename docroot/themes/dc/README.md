# Theme for Drupal Challange

## Installation

Run `npm install`, `gulp copy-assets` will beperformed automatically, so you don't have to

## Usage

You can watch for any changes to your files by running:

- `gulp watch`
